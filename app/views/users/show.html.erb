<% provide(:title, @user.user_name) %>

<div class="row">
  <%= render 'shared/user_info' %>
  <div class="col-4 col-md-12">
    <% if @user.photos.any? %>
      <ol class="photos">
        <%= render @photos %>
      </ol>
      <%= will_paginate @photos %>
    <% end %>
  </div>
</div>
