<%= form_for(current_user.active_user_relationships.find_by(followed_id: @user.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "フォロー解除", class: "btn btn-secondary" %>
<% end %>
