<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.user_name %></h1>
<span><%= link_to "自分のプロフィール", current_user %></span>
<span><%= pluralize(current_user.photos.count, "Photo") %></span>
